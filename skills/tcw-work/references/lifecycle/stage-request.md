# Stage: request

## Purpose

Capturing the request.
Get your instructions on how to produce the output by running
`tcw work stage begin request <slug>`.

## Inputs

`intake.md`, when the item started from one.

## Produce

`initial-request.md` — and, for an epic, the coordination goal it alone states:
what the children are collectively for. See [`epic-deltas.md`](../epic-deltas.md).

## Steps

1. **Not delegable**, and uniquely so among the stages: it exists to get input
   only the user has, and a subagent cannot ask them. Run it inline under every
   harness. — user `[judgment]`
2. Commit the artifact as you write it; nothing enforces that. — agent
   `[judgment]`
