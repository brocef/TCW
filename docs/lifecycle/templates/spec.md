# Spec — <Title>

## Capability changes

_Planned ledger deltas only — new, changed, removed, or none. No records are
written here._

## Problem

_What is wrong today, grounded in the code with file and line. A claim recalled
rather than checked is how a spec starts lying._

## Goals

## Non-goals

## Design

_For an epic, replace this with the child boundaries and their ordering
constraints._

## Abstraction litmus test

_Name the operations this spec adds or changes, and give each a verdict:_
_model / store interface, or filesystem-adapter private detail. See_
_[`../abstraction.md`](../abstraction.md). "No new operation" is a valid answer_
_and still has to be written._

## Acceptance criteria

_Each one checkable by someone else without asking what it meant._

### Coverage

_If the Design section numbers anything — resolution rules, command steps, error
surfaces, failure modes — cross every criterion against those numbers in one
table here. A cell is a test name, or `n/a` **with the `file:line` that makes it
n/a**._

_"This criterion doesn't reach rule 4" is a claim; the line that proves it is the
check. A cell filled from memory is how four consecutive defects shipped in the
store-provisioning epic — every one of them would have been written `n/a` in
good faith. A blank cell is the finding._

_The axes are lifted, not invented: they come from a numbered list already in
this document, so there is no judgment about what to vary — only about what each
cell holds. Writing criteria as general properties is **not** a substitute; it
was tried, and the narrowing simply moved into the test fixtures._

| # | rule 1 | rule 2 | rule 3 | rule 4 |
| - | ------ | ------ | ------ | ------ |

## Risks

## Notes

_Optional._
