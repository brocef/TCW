Choose Light, Dark, or System appearance for the local web app. The preference
is stored in the current browser. System is the default and follows the
operating system's current color-scheme preference.
