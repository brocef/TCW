# Upcoming

Developer changelog for the next version. Technical and precise; grouped by
category, with commit hash ranges so entries trace back to source.

## Added

- New `tcw-report` skill (`skills/tcw-report/SKILL.md`): routes TCW users to
  file bugs and suggestions as GitHub issues on the TCW repo, with fill-in
  skeletons for bug reports (environment, steps to reproduce, expected vs.
  actual, remediation) and suggestions/features (motivation, description,
  benefits). Added it to the `tcw-plugin` skill map and the README skills
  section. (15974ed..ba203a6)

## Fixed

- `.codex-plugin/plugin.json` `longDescription` now enumerates all five skills;
  it previously listed only three, omitting `tcw-taxonomy` and `tcw-report`.

## Internal

- Backfilled `spec.md` / `plan.md` for the `tcw-report` work item so the
  completed folder carries the lifecycle artifacts its DoD implies.
