# Upcoming

User-facing release notes for the next version. Plain language — no jargon or
internal module names.

## New features

- Large work items can split an implementation plan into declared stage
  documents. The work view shows dependencies and lets you create, edit, delete,
  or open each document independently, while existing single-file plans continue
  to work unchanged.
