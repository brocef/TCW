# Upcoming

User-facing release notes for the next version. Plain language — no jargon or
internal module names.

## Changed

- A fresh install of the TCW plugin now runs `tcw` commands without asking for
  permission on every command — the skills ship with the permissions they need
  already declared.
