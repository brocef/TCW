# Upcoming

User-facing release notes for the next version. Plain language — no jargon or
internal module names.

- `tcw work new` and `tcw work start` now tell you the next step to run, so it's
  harder to forget to start an item before you code it or to mark it done when you
  finish. The reminder prints alongside the command's normal output.
