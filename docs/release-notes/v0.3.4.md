# Upcoming

User-facing release notes for the next version. Plain language — no jargon or
internal module names.

## Added

- When you create a work item with `tcw work new`, it now shows the path to the
  item's content file so you (or an agent) can open it for editing right away —
  no need to look up the path separately.
