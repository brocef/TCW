# Upcoming

User-facing release notes for the next version. Plain language — no jargon or
internal module names.

- Agents now commit each work item's request, specification, plan, outcome, and
  refined outcome as separate lifecycle checkpoints instead of leaving those
  files uncommitted or batching several stages together.
