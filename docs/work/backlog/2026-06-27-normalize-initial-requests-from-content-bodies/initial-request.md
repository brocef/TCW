# Initial request — Normalize initial requests from content bodies

## Requested outcome

Migrate all of this repo's TCW work item `content.md` files to be the
corresponding `initial-request.md` files.

## Constraints

- Normalize every existing work item, including ones that already have an
  `initial-request.md`.
- Keep `content.md` in place for now because the current CLI still uses it as the
  item body/overview surface.
