# Remote adapter: JiraWorkStore

## Product changes

## Technical changes

## Meta changes

Remote `WorkStore` adapter for an external tracker (Jira or any equivalent). Purely additive -- the abstract `WorkStore` interface already exists; this is the whole point of the storage abstraction.

Spec: docs/plan/phase-6-beyond.md; phase-5-work Part C ("beyond the roadmap").
