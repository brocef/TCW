# Refined outcome

Accepted by the user on 2026-09-25, including keeping vitest 4.1.11.

- GitHub showed 0 open Dependabot alerts after the push. The CI `test` run on
  `7504fa73` passed web-build and pytest on Python 3.11 and 3.14.
- The fixes reach users only in the next release. No GitHub issue originated this
  item, so there is nothing to close.
- Follow-up: the two end-to-end tests that fail, and the unstable theme
  screenshot, are being fixed separately at the user's request.
