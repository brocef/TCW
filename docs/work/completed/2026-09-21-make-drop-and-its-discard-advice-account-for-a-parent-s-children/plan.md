# Plan

1. Tests first, in `tests/test_work.py` beside the drop tests: criteria 1–4,
   watched failing.
2. `tcw/work/cli.py` `_drop`: the descendant refusal before the gate, and the open
   children in the non-backlog advice. `tcw/store/base.py` `drop`: the same
   open/resolved wording. `--parent` help text.
3. Documentation Sync: a changelog Fixed line. The release notes' drop section
   gets one sentence. The README drop row already says drop refuses; no change.
   Skill and guides: `skills/work/references/procedures/decompose.md` and
   docs/guide/work.md already say a parent with children cannot be dropped; no change.

## Verification

Run the drop and child tests, and `test_documented_cli_surface`. By hand, in a
scratch node: a parent with an open child, then with a resolved child; drop each.
