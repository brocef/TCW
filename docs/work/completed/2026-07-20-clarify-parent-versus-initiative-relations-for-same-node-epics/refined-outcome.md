The user verified the guidance correction and approved completion on 2026-07-20.

No refinements were requested. The final implementation keeps `--parent` for
coupled nested decomposition and documents `--initiative` for independently
scheduled epic tasks in the same node or across registered nodes.

Final verification remains clean: `tcw validate` and `git diff --check` passed.
GitHub issue #6 may be closed as completed after the TCW completion transition.
