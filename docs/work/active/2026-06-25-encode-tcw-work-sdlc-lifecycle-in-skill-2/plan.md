# Plan — encode the TCW work SDLC lifecycle in the skill

1. Update the plugin capability ledger for the new planning/execution command affordance.
2. Add `skills/tcw-work/docs/lifecycle.md` with the full SDLC, artifact contract, compression rule, and closeout decisions.
3. Update `skills/tcw-work/SKILL.md` to make the lifecycle the primary work-driving model and route detailed work to the new sub-document.
4. Add `/tcw-plan-work` and `/tcw-drive-work-to-completion` command markdown wrappers.
5. Update public docs and upcoming release/changelog files if documentation sync triggers fire.
6. Verify skill docs and command docs with targeted reads plus the relevant test subset.
