I want a TCW work item that lets projects specify another repository as the location of its work items and artifacts.

The issue I'm having is that one of my projects stores its work items inside another orchestrator folder on my machine, however when I start a cloud session in Claude Code it only clones that one project, so all of the work artifacts are missing.
