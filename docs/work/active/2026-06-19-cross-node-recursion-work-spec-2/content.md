# Cross-node recursion (work Spec 2)

## Product changes

## Technical changes

## Meta changes

Node discovery; epics + `initiative:` back-pointers; `reconcile` (scan child nodes -> consolidated rollup); escalate/delegate over the inbox channel; `tcw work start --worktree` plus the rule for which checkout owns docs/work/ writes; the two-layer x two-layer capability mapping (epic <-> product-layer ledger, task <-> leaf-node ledger). Deferred until the single-node core is dogfooded.

Spec: docs/plan/phase-6-beyond.md (work Spec 2); phase-5-work A.2/A.6/A.8, Part C #2.

